
@{
}
@model Book


    <div class="card m-1 p-1 border-dark">
        <div class="bg-opacity-75 p-1">
            <h4 class="card-header">@Model.Title</h4>
            <span class="badge bg-dark" style="float:right">Price: $@Model.Price</span>

        </div>


        <ul>
            <li class="list-group-item">Author: @Model.Author</li>
            <li class="list-group-item">Publisher: @Model.Publisher</li>
            <li class="list-group-item">ISBN: @Model.Isbn</li>
            <li class="list-group-item">Classification: @Model.Classification</li>
            <li class="list-group-item">Category: @Model.Category</li>
            <li class="list-group-item">@Model.PageCount pages</li>

        </ul>
        <form method="post" asp-page="/Add">

            <input type="hidden" asp-for="@Model.BookId"/>
            <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />

            <span class="card-text">
                <button type="submit" class="btn btn-primary btn-sm float-end">Add to cart</button>
            </span>
            
        </form>
    </div>
